apiVersion: 1

providers:
  - name: "stocks-provider"
    orgId: 1
    folder: "Stocks Dashboards"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 5
    options:
      path: /var/lib/grafana/dashboards
